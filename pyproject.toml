[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ai-uvp"
version = "2026.7.0"
description = "uvp - Vibe Coding 工作流工具"
requires-python = ">=3.10"
dependencies = [
    "mkdocs-material",
    "mkdocs-glightbox",
    "mkdocs-awesome-pages-plugin",
    "neoteroi-mkdocs",
]
