  严重冲突（行为不符）

  ┌─────┬─────────────────┬──────────────┬────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │  #  │      涉及       │     问题     │                                                                  详情                                                                  │
  ├─────┼─────────────────┼──────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │     │                 │ common.rs vs │ 代码定义的合法状态为 idea, planned, in_progress, implemented, verified, paused, deprecated, removed。但 FEAT-016 skill（实际部署给 AI  │
  │ 1   │ 状态枚举不一致  │  FEAT-016    │ 使用的）定义了完全不同的流程：idea → planned → implementing → verifying → verified → closed。两套状态名互相不兼容（in_progress vs      │
  │     │                 │ Skill        │ implementing；implemented vs 没有对应；代码中没有 closed/verifying）。AI 按 skill 操作会写入代码不认识的状态值。                       │
  ├─────┼─────────────────┼──────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 2   │ feature_new     │ FEAT-016 vs  │ Skill 说 uvp f new 创建时状态为 idea，但代码实际写入 planned（见 feature.rs:68）。                                                     │
  │     │ 起始状态        │ 代码         │                                                                                                                                        │
  ├─────┼─────────────────┼──────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │     │                 │ FEAT-009     │                                                                                                                                        │
  │ 3   │ --fix 为空实现  │ spec vs      │ Spec 定义了 --fix 应自动修复（如重新生成 registry.md），但代码中参数名为 _auto_fix，完全无逻辑。                                       │
  │     │                 │ check.rs     │                                                                                                                                        │
  ├─────┼─────────────────┼──────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │     │ --onboard       │ FEAT-006     │                                                                                                                                        │
  │ 4   │ 是空壳          │ spec vs      │ Spec 描述了完整的在线实时状态面板行为，但代码只打印 "coming soon" 就 return。                                                          │
  │     │                 │ status.rs    │                                                                                                                                        │
  ├─────┼─────────────────┼──────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 5   │ feature status  │ FEAT-004     │ Spec 要求非法状态转换产生错误并提示合法路径，但代码接受任意字符串作为新状态，不做校验。                                                │
  │     │ 无状态校验      │ spec vs 代码 │                                                                                                                                        │
  └─────┴─────────────────┴──────────────┴────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘


决策：
1. 枚举状态以`idea → planned → implementing → verifying → verified → closed`这一套为准，将冲突的都适配到这个流程
2. uvp f new 创建时状态为 planned
3. 更新自动修复的逻辑
4. 暂时不修改，保持占位
5. 加入校验

中等冲突（功能缺失/部分实现）

  ┌─────┬────────────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │  #  │              涉及              │                                                                  问题                                                                   │
  ├─────┼────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 6   │ --quiet/--verbose              │ main.rs 定义了这两个 flag 但从未传给任何命令处理函数。                                                                                  │
  │     │ 全局参数为死代码               │                                                                                                                                         │
  ├─────┼────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 7   │ generate_meta_header()         │ FEAT-008 spec 说此函数用于生成文件头，但代码中没有任何命令实际调用它。feature_new 和 adr 命令都用内联模板而非此函数。                   │
  │     │ 从未被调用                     │                                                                                                                                         │
  ├─────┼────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 8   │ feature_list UTF-8 截断 panic  │ feat.title[..26] 对中文标题会 panic（多字节字符边界截断）。项目标题均为中文，这是个必现 bug。                                           │
  ├─────┼────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 9   │ render --check 每日报假阳性    │ 因为 front matter 中 date: 使用当天日期，隔天检查一定报 "inconsistent"，即使数据没变。与 FEAT-011 spec 要求的"幂等性"矛盾。             │
  ├─────┼────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 10  │ obsidian pull 路径与 spec 不符 │ FEAT-005 spec 说从 <vault>/Projects/<project-name>/reference/ 拉取，但代码实现是直接从 vault 根路径拉取到 reference/，没有              │
  │     │                                │ Projects/<project-name>/ 这层子路径逻辑。                                                                                               │
  ├─────┼────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 11  │ Config 布尔值合并 bug          │ FEAT-007 说项目配置优先于全局配置，但 merge_configs 对布尔值只在 other=true 时覆盖，无法在项目级设置 auto_uv_init = false 来覆盖全局的  │
  │     │                                │ true。                                                                                                                                  │
  └─────┴────────────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

决策：
6. 去掉--quiet/--verbose相关的代码
7. 使用内联模板是正常的。但generate_meta_header()应该被使用，并且我在实际使用中发现了这个问题，在AI自动生成文档时，并没有按照预设的文件头生成。顺带检查一下`src/ruvp/skills/uvp-meta-header`这个skill是否会被触发。这个可以后续再讨论
8. 修复feature_list UTF-8 截断 panic
9. 修复"render --check 每日报假阳性 "
10. 修复“obsidian pull 路径与 spec 不符”的问题
11. 修复Config 布尔值合并 bug


  轻微不一致（文档过时或 spec 过度描述）

  ┌─────┬──────────┬────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │  #  │   涉及   │                                                            问题                                                            │
  │ 12  │ FEAT-012 │ Spec 说部分命令需迁移到 ui.rs API，仍有 adr、feature new、obsidian、status --open、check、render 使用原始 console::style。 │
  ├─────┼──────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 13  │ FEAT-006 │ Spec 说 --help 附带工作流编排提示并打开 workflow.svg，但代码中 --help 由 clap 自动处理，无自定义行为。                     │
  ├─────┼──────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 14  │ FEAT-014 │ todo_cmd.rs 中非匹配格式的行在 rebuild 时会被丢弃（数据丢失风险），spec 未提到应保留手写内容。                             │
  ├─────┼──────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 15  │ FEAT-010 │ Spec 提到 Python wrapper uv-plus 分发路径，但当前项目完全是 Rust 实现，无 Python 包装层代码存在于仓库中。                  │
  └─────┴──────────┴────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
  

决策：
12. 全部都要迁移到 ui.rs API
13. 这个问题可以忽略，不用打开workflow.svg了
14. 修复丢弃风险
15. 检查下现在代码是否可以通过`pip install uvp`实现安装